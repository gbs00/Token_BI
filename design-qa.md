# 菜单栏设计核验

日期：2026-09-12。结果：通过本轮本地视觉核验；不替代安装与真实副屏验收。

## 输入与截图

- 已选原图：`docs/design-previews/menubar-choice-1.png`，方案 1「静谧列表」。没有采用方案 2/3。
- 正常面板原图区域 x=28、y=196、366 × 706；用 HTML overflow 展示该区域，没有重画原图。
- 实现 iframe 同为 366 × 706，在同一截图内并排比较：[对比](docs/design-previews/menubar-qa/comparison-normal.png)。
- 实现全图：[正常](docs/design-previews/menubar-qa/normal.png)、[紧凑](docs/design-previews/menubar-qa/compact.png)。

## 核验结果

- 中性深灰、纵向列表、分隔线、蓝色扫码主按钮、灰色打开按钮和三个底部动作保持设计方向。
- 两窗口等权重，百分比和条形比例一致，重置剩余时间右对齐；无额外卡片嵌套或大标题。
- 长账号省略且保留完整 title；0% 不消失，只有周额度时不伪造 5h。字号、按钮尺寸、留白与原图基本一致。
- 在 in-app browser 检查正常、未登录、失败及 QR 页；在真实 Tauri WebView 检查额度、二维码、固定入口、复制成功与失败文案。
- 320 × 480、366 × 400 无横向溢出，内容可滚动且底部操作可见。语义按钮、aria 状态、键盘焦点及 Escape 均有覆盖。

## 差异与修正

- 保留项目现有蓝色品牌图标，而非生成图重绘版本。
- 删除功能副标题、重复时间；真实 QR 下展示连接条件，不出现“演示二维码”。
- 减少拟物高光，使用非透明深灰背景；系统字体与生成图字体略有差异，信息层级和布局一致。
- 原生检查发现本次新增桥接客户端缺少 TLS provider 初始化，已修复并补回归；这不是对旧版启动失败原因的重新归因。

## 验收边界

物理多屏/全屏 Space、真实菜单栏点击与焦点切换的长期使用、睡眠唤醒、iPhone 扫码和 LAN 连续同步待验收。自动化 App 激活会重新聚焦窗口，因此不能只凭该工具截图证明失焦收起行为；本轮以事件处理和隐藏状态回归覆盖，仍需实机验收。

没有安装替换、发布或 GitHub 推送。详情见 [实施纪要](docs/TECH_MENUBAR.md)。
