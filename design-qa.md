# 菜单栏设计核验

本文件保留各轮本地核验边界；最终单色双环图标已纳入用户授权的 v1.2.5 发布范围，发布验证另见 [更新说明](docs/RELEASE_NOTES_v1.2.5.md)。历史“未发布”描述不代表后续发布状态。

日期：2026-09-12。结果：通过本轮本地视觉核验；不替代安装与真实副屏验收。

## 输入与截图

- 已选原图：`docs/design-previews/menubar-choice-1.png`，方案 1「静谧列表」。没有采用方案 2/3。
- 正常面板原图区域 x=28、y=196、366 × 706；用 HTML overflow 展示该区域，没有重画原图。
- 实现 iframe 同为 366 × 706，在同一截图内并排比较：[对比](docs/design-previews/menubar-qa/comparison-normal.png)。
- 实现全图：[正常](docs/design-previews/menubar-qa/normal.png)、[紧凑](docs/design-previews/menubar-qa/compact.png)。

## 核验结果

- 中性深灰、纵向列表、分隔线、蓝色扫码主按钮、灰色打开按钮和三个底部动作保持设计方向。
- 两窗口等权重，百分比和条形比例一致，重置剩余时间右对齐；无额外卡片嵌套或大标题。
- 长账号省略且保留完整 title；0% 不消失，只有周额度时不伪造 5h。字号、按钮尺寸、留白与原图基本一致。
- 在 in-app browser 检查正常、未登录、失败及 QR 页；在真实 Tauri WebView 检查额度、二维码、固定入口、复制成功与失败文案。
- 320 × 480、366 × 400 无横向溢出，内容可滚动且底部操作可见。语义按钮、aria 状态、键盘焦点及 Escape 均有覆盖。

## 差异与修正

- 保留项目现有蓝色品牌图标，而非生成图重绘版本。
- 删除功能副标题、重复时间；真实 QR 下展示连接条件，不出现“演示二维码”。
- 减少拟物高光，使用非透明深灰背景；系统字体与生成图字体略有差异，信息层级和布局一致。
- 原生检查发现本次新增桥接客户端缺少 TLS provider 初始化，已修复并补回归；这不是对旧版启动失败原因的重新归因。

## 验收边界

物理多屏/全屏 Space、真实菜单栏点击与焦点切换的长期使用、睡眠唤醒、iPhone 扫码和 LAN 连续同步待验收。自动化 App 激活会重新聚焦窗口，因此不能只凭该工具截图证明失焦收起行为；本轮以事件处理和隐藏状态回归覆盖，仍需实机验收。

没有安装替换、发布或 GitHub 推送。详情见 [实施纪要](docs/TECH_MENUBAR.md)。

## 菜单栏图标补充核验（2026-09-24）

本节仅对应用户新选定的第 2 版「双层额度环」，不改变上述历史面板验收结论。

- 已制作独立单色模板并接入 macOS 托盘，彩色应用图标及原交互保持不变。
- 生产 PNG 通过像素检查；1×/2× 的 AppKit 颜色与 alpha 离屏观察可辨认双环及右上开口。Rust 20 项、格式及 Clippy 检查通过。
- 根据用户反馈微加粗描边，约由 1pt 调整至 1.1–1.2pt；18pt 显示框保持不变。[前后对比](docs/design-previews/menubar-template-weight-comparison.png) 中双环仍分离。
- 该离屏观察不是实际菜单栏截图。用户确认约 1.2pt 后已打包替换本机 App；真实 NSStatusItem 的系统着色、选中状态与跨屏表现仍未由自动化验证。
- 内置浏览器的 URL 策略阻止本地 HTML 预览，未使用其他浏览器或本地服务器绕过。因此浏览器视觉 QA 尚未通过。
- 资源、预览与具体边界见 [图标记录](docs/design-previews/menubar-template.md)。

- 本机安装包签名和构建/安装哈希核对通过，Rust 20 项、桌面模型 13 项及隔离打包集成检查通过。原生工具查看安装后额度面板正常，账号数据保留，8790/8787 服务健康。
- 本次只完成本机替换，保留旧包备份，不提交、推送或发布。
- 用户随后要求试用约 1.4pt：已替换生产资源、同步离屏预览并再次安装本机。Rust 20 项、隔离打包检查、签名与构建/安装哈希核对通过；原生面板中账号、额度和存储重置正常。[1.2pt / 1.4pt 对比](docs/design-previews/menubar-template-weight-14-comparison.png) 可见线条加粗且双环仍分离，线宽近似值与安装备份见图标记录。
- 最新按用户要求整体等比例放大约 8%：通过居中裁减高分辨率源图的透明留白重新导出，不重画。可见图形未裁切，直径、描边和间距同步放大。[尺寸对比](docs/design-previews/menubar-template-scale-comparison.png) 已保存；再次完成 20 项 Rust 测试、打包检查和本机替换，原生账号面板与服务健康检查通过。
- 用户进一步要求缺口与原应用图标一致：核对原图后将模板逆时针旋转 90°，改为左上开口，其余尺寸和描边不变。[方向对比](docs/design-previews/menubar-template-direction-comparison.png) 已保存；20 项测试、打包检查、签名和安装哈希核对通过，本机已替换，账号面板和服务正常。原彩色图标保持不变。

final result: 本机替换及回归通过；系统菜单栏真实深浅色、选中和跨屏效果待用户实机验收，HTML 浏览器视觉 QA 仍受 URL 策略阻止。
