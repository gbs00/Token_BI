# v1.2.6 发布回执

## 发布信息

- 用户明确授权正式发布；发布前另行要求补齐正式 WKWebView 组件真人登录，现已通过。
- 发布时间：2026-09-25 20:34:27（Asia/Shanghai）；UTC：2026-09-25T12:34:27Z。
- [Release](https://github.com/gbs00/Token_BI/releases/tag/v1.2.6)，Release ID：`396589111`；非草稿、非预发布，已设为 GitHub 最新版。
- 发布源码：[`73ed45003ce8edaf644d81eb1e1eebdf8144bd3a`](https://github.com/gbs00/Token_BI/commit/73ed45003ce8edaf644d81eb1e1eebdf8144bd3a)，标签 `v1.2.6`。本回执为发布后文档补录，不移动已发布标签。
- [更新说明](../RELEASE_NOTES_v1.2.6.md)、[原生网页登录接入](../TECH_WKWEBVIEW_PRODUCTION.md)、[Python 构建依赖快照](v1.2.6-python.txt)。

## 验证结果

- 本地完整构建已通过 `scripts/release_local.sh`；正式发布继续使用本机已安装验收的精简包，没有重新生成不同二进制。
- 发布前复跑：Python/网页/原生 WKWebView 488 项通过（167.94 秒），JavaScript 13 项、Rust 28 项通过；1 项显式视觉导出测试默认忽略。依赖、格式和 Clippy `-D warnings` 通过。
- 正式包内 WKWebView 由用户手动登录同一账号，用户确认成功后窗口自动收起。隐藏读取、连续读取、关闭组件进程后重启读取均返回成功，同账号身份及额度、存储重置解析验证通过；不使用 Probe 会话替代。
- 实机原始额度报告留在本地忽略目录，不进入仓库或 Release；Token/Cookie 不通过原生桥导出，不上传个人账号及实际额度数据。
- App 深度签名、DMG 校验、全部打包资源、单 Python 运行库及归档模块排除检查通过。
- 打包 control/backend 健康、暂停接入、看板资源、二维码、隔离 OAuth 请求、失败保留缓存、恢复、退出和端口释放通过。
- Tauri 官方 Updater 对最终清单完成签名下载、篡改拒绝及临时 App 安装 1 项验证；旧双运行库目录被替换，新运行库链接完整。没有关闭 HTTPS 或签名校验。
- 草稿阶段逐项核对五个附件的上传状态、大小、GitHub SHA-256 摘要及说明正文，一致后才发布。
- 公开 `releases/latest/download/latest.json` 无认证下载成功，与本地清单逐字节一致；版本、平台、URL 与签名均匹配。进一步无认证下载完整更新归档，字节数和 SHA-256 与本地一致。
- 主分支与标签已推送，远端标签指向发布源码。GitHub Release Actions 仍停用，沿用原 Updater 密钥，没有上传私钥。

## 制品清单

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `Token.BI_1.2.6_aarch64.dmg` | 17832156 | `bcb1332524140b6134227085d6cc48d11c929a676c82270e15a3e85eee3f5bac` |
| `Token.BI_1.2.6_aarch64.app.tar.gz` | 17443457 | `1d7e0990fbe641fbf8751df45bae7f98bf79e206a139fca14b4bc87cb2722d7b` |
| `Token.BI_1.2.6_aarch64.app.tar.gz.sig` | 408 | `6249ea04ea7ab4334c579f183d8d845ddfe8479ad5359f50084ce850a981bc2b` |
| `latest.json` | 3368 | `10be0d5eb50e886f0360f6fdfdea0f892516bce3ad295d0d3d5d5250b8d02f32` |
| `SHA256SUMS` | 375 | `cb930e4ce690159bea674bd3761bc348195f7b6f9d35411fa8c2f1e75de1e333` |

App 为 40,142,282 字节，104 个实体文件、4 个符号链接，符号链接不重复计入。相较正式 1.2.5，App 减少 78.2%，DMG 减少 71.6%；两服务仍为独立进程。共享运行库精简前后启动基准基本持平，不宣称整体启动、内存或耗电按包体比例改善。

## 本机状态

- 发布前已将相同精简包安装到 `/Applications/Token BI.app`；版本为 1.2.6，最低 macOS 11.0。
- 账号及接入状态保留，真实 OAuth 同步正常，菜单栏额度、存储重置、看板及二维码验证通过；正常 OAuth 路径未预加载网页登录组件。
- 已安装主程序 SHA-256：`358c0a4980d9a1756ade804e31a1b3c306b4420c07e134fd5d410f804da61a19`，与发布更新归档中的主程序一致。
- 实机网页登录验证完成后关闭测试子进程，正式 WebKit 会话保留；不安装独立 Probe，不再次覆盖用户应用。
- [本机安装及精简基线](v1.2.6-shared-runtime.md)、[首轮基线及正式登录补验](v1.2.6-verification.md)。

## 验收边界

- 本次实际系统为 macOS 27 Apple Silicon；旧 macOS、第三方 SSO、同邮箱多组织、长期会话过期、真实睡眠和跨设备网络切换仍需扩大验收。
- 断网、401、限流、唤醒调度及进程异常有隔离测试，不等同于长时间实机场景；本轮未从旧版已安装 App 执行完整线上升级，只进行了官方 Updater 临时安装验证和公共下载校验。
- 仅 Apple Silicon，App 为 ad hoc 签名、未公证；Updater 签名不等于 Apple Developer ID 信任。
- 无关 `Design/` 未修改或提交；生产 Playwright/Node 已移除，Playwright 仅为开发测试依赖。
