# v1.2.5 发布回执

## 发布信息

- 用户明确授权正式发布，非草稿、非预发布，已设为 GitHub 最新版。
- 发布时间：2026-09-24 21:21:20（Asia/Shanghai）；UTC：2026-09-24T13:21:20Z。
- [Release](https://github.com/gbs00/Token_BI/releases/tag/v1.2.5)，Release ID：`395712596`。
- 发布源码：[`3131778272699cb5c1ced6904df99d53f9d8cc3a`](https://github.com/gbs00/Token_BI/commit/3131778272699cb5c1ced6904df99d53f9d8cc3a)，标签 `v1.2.5`。本回执为发布后文档补录，不移动已发布标签。
- [更新说明](../RELEASE_NOTES_v1.2.5.md)、[图标设计记录](../design-previews/menubar-template.md)、[Python 构建依赖快照](v1.2.5-python.txt)。

## 验证结果

- `scripts/release_local.sh` 全部通过：Python/Chromium/WebKit 425 项（137.74 秒）、JavaScript 13 项、Rust 20 项，以及真实签名更新归档测试 1 项。
- 格式、Clippy `-D warnings`、依赖与 Git 差异检查通过；最终更新说明写入 manifest 后，更新归档测试再次通过。
- 模板的 36×36 尺寸、单色像素、透明边界和中心检查通过；最终模板为 1,644 字节，1×/2× 离屏预览确认双环和左上开口可辨认。
- 完整重建 shell、control、backend。App 为 184,506,082 字节（符号链接不重复计入），运行库链接与 195MB 预算检查通过。
- 打包服务健康、暂停账号接入、看板资源、二维码以及停止后端口释放通过。
- App 深度签名、DMG 校验、只读挂载后的 App 签名通过；镜像已卸载。
- Tauri 官方 Updater 有效签名下载、篡改拒绝和临时 App 安装通过；安装后的 Python 链接与 codesign 验证通过。
- 草稿阶段核对全部 5 个附件的上传状态、大小、GitHub SHA-256 摘要与更新说明正文，一致后正式发布。
- 公开 `releases/latest/download/latest.json` 无认证获取成功，与本地清单逐字节一致；版本为 `1.2.5`，平台为 `darwin-aarch64`，归档地址与签名匹配。
- GitHub 旧 Release Actions 工作流保持停用；沿用原 Updater 签名密钥，未上传私钥。

## 制品清单

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `Token.BI_1.2.5_aarch64.dmg` | 62854710 | `7e6e37f7e482be410402181ec418451ccb39ec098b47aad7c141fa246f925a8d` |
| `Token.BI_1.2.5_aarch64.app.tar.gz` | 63794138 | `5ca1c4cbe9dbfe58183648235bfab67a9bd93167c7d39359a0b122b4c61af365` |
| `Token.BI_1.2.5_aarch64.app.tar.gz.sig` | 408 | `463b98535bcadbe3b9d4017fc3b149df2dcd6a78f3ff20fe7321351b0dce1687` |
| `latest.json` | 3351 | `a66735dbeea6c3d7d2569d90b62fe973d8eedb117024cb9b5d71b44f24edf569` |
| `SHA256SUMS` | 375 | `5e5b47f0b207fe6b2514e8e1c9dccb0456d883f0a168db6819c1b646563d8eea` |

## 本机升级验证

- 已从 `/Applications/Token BI.app` 的 1.2.4 本地验收版，通过真实设置页面完成「检查更新 → 立即更新 → 签名校验通过 → 重启完成更新」，未采用手动覆盖代替此验证。
- 重启后设置显示 `1.2.5` 与「已是最新版本」；额度面板继续显示同一脱敏账号、OAuth、额度和存储重置，未执行退出账号或兑换重置操作。
- 控制台健康接口确认打包模式与主服务运行，主服务健康接口版本为 `1.2.5`；原进程退出，仅有一套已安装 App 的 shell、control、backend。
- 安装目录通过 `codesign --verify --deep --strict`。按常规文件内容、权限及符号链接目标比对，516 个文件和 8 个符号链接与正式构建完全一致。
- 已安装可执行文件 SHA-256：`8ce5840046229cdc49894369a9c52aef2d4061ea57ce51d0a51f22e83eb17543`。
- 升级前旧 App 保留在忽略目录 `dist/Token BI-before-v125-official-20260924.noindex`；未清理真实账号、设置或浏览器数据。

## 验收边界

- 本次仅替换 macOS 菜单栏模板；彩色应用图标、账号优先级、数据同步、副屏功能及更新逻辑不变。
- 离屏图标对比不等同于真实系统菜单栏截图；未完成所有系统外观、菜单栏高亮、物理多屏、iPhone 真机、长期睡眠恢复和真实网页登录验收。
- 未跟踪的 `Design/` 不进入提交；生产 Playwright 保留，CDP 选型与重构仍安排后续版本。
- 仅 Apple Silicon，App 为 ad hoc 签名，未做 Apple Developer ID 签名或公证。Updater 签名不代替 Gatekeeper 信任。
